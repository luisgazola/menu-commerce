'use client'; import {useState} from 'react'; const API=process.env.NEXT_PUBLIC_API_URL??'http://localhost:3001/api/v1'; export default function Page(){const [storeId,setStoreId]=useState('');const [message,setMessage]=useState('');async function save(e:any){e.preventDefault();const token=localStorage.getItem('menu-commerce-token');const r=await fetch(`${API}/admin/stores/${storeId}/payments`,{method:'PATCH',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({provider:'MOCK',enabled:true,sandbox:true,pixEnabled:true,creditCardEnabled:true,debitCardEnabled:false,cashEnabled:true,cardOnDeliveryEnabled:true})});setMessage(r.ok?'Configuração salva.':'Falha ao salvar.')}return <main className="admin-shell"><h1>Pagamentos</h1><p>Configure o gateway em homologação antes de usar credenciais de produção.</p><form onSubmit={save} className="admin-card"><label>ID da loja<input value={storeId} onChange={e=>setStoreId(e.target.value)} required/></label><button>Salvar configuração MOCK</button><p>{message}</p></form></main>}
